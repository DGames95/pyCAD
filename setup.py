from setuptools import setup

setup(
    name='pyCAD',
    version='1.0',
    description='command line CAD to create 3D geometry',
    author='Damian Games',
    packages=['pyCAD'],
    install_requires=[
        'numpy',
    ],
)
